# Direct dependencies for the MCP bridge and its control panel.
# Everything else in the venv is transitive.
# Pinned to the versions known to work as of 2026-08-11.
mcp==2.0.0
psutil==7.2.2
pydantic==2.13.4
starlette==1.6.0
uvicorn[standard]==0.52.1
